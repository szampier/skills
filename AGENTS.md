Skills are organized in bucket folders under `skills/`:

- `archive/` — skills for querying the ESO Science Archive and related data services
- `misc/` — kept around but rarely used
- `deprecated/` — no longer used

Every skill in `archive/` or `misc/` must have a reference entry in the top-level `README.md`.

Each bucket folder has a `README.md` that lists every skill in the bucket with a one-line description, with the skill name linked to its `SKILL.md`.

Each skill entry in the top-level `README.md` must link the skill name to its `SKILL.md`.

Skills in `deprecated/` must not appear in `README.md`.
